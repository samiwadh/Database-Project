CREATE TABLE  "TRAIN_STATUS" 
   (	"TRAIN_DATE" VARCHAR2(34), 
	"AC_SEAT" VARCHAR2(34), 
	"GEN_SEAT" VARCHAR2(34), 
	"B_AC_SEAT" VARCHAR2(34), 
	"B_GEN_SEAT" VARCHAR2(30), 
	"DEPARTURE_T" VARCHAR2(30), 
	"STATION" VARCHAR2(30), 
	"TRACK_ON" VARCHAR2(30), 
	"TRAIN_NUMBER" NUMBER(5,0), 
	"BOOKING" VARCHAR2(30), 
	"TRAIN_ID" NUMBER(5,0), 
	 CONSTRAINT "TRAIN_STATUS_CON1" PRIMARY KEY ("TRAIN_ID") ENABLE
   ) ;

ALTER TABLE  "TRAIN_STATUS" ADD CONSTRAINT "TRAIN_STATUS_CON" FOREIGN KEY ("BOOKING")
	  REFERENCES  "PASSANGER" ("BOOKING") ENABLE;
ALTER TABLE  "TRAIN_STATUS" ADD CONSTRAINT "TRAIN_STATUS_FK" FOREIGN KEY ("TRAIN_NUMBER")
	  REFERENCES  "TRAIN_LIST" ("TRAIN_NUMVER") ENABLE;



CREATE TABLE  "TRAIN_LIST" 
   (	"TRAIN_NUMVER" NUMBER(6,0), 
	"TRAIN_NAME" VARCHAR2(30), 
	"AC_FARE" VARCHAR2(30), 
	"GEN_FAN" VARCHAR2(30), 
	"MON_AVAILAVLE" VARCHAR2(30), 
	"SUN_AVAILABLE" VARCHAR2(30), 
	"SOURCE" VARCHAR2(30), 
	"DESCRIPTION" VARCHAR2(30), 
	 CONSTRAINT "TRAIN_LIST_PK" PRIMARY KEY ("TRAIN_NUMVER") ENABLE
   ) ;

CREATE OR REPLACE TRIGGER  "BI_TRAIN_LIST" 
  before insert on "TRAIN_LIST"               
  for each row  
begin   
  if :NEW."TRAIN_NUMVER" is null then 
    select "TRAIN_LIST_SEQ".nextval into :NEW."TRAIN_NUMVER" from dual; 
  end if; 
end; 

/
ALTER TRIGGER  "BI_TRAIN_LIST" ENABLE;




 









Table 

Data 

Indexes 

Model 

Constraints 

Grants 

Statistics 

UI Defaults 

Triggers 

Dependencies 

SQL 

 
CREATE TABLE  "PASSANGER" 
   (	"NAME" VARCHAR2(30), 
	"SEX" VARCHAR2(30), 
	"AGE" VARCHAR2(30), 
	"ADRESS" VARCHAR2(30), 
	"STATUS" VARCHAR2(30), 
	"CATEGORY" VARCHAR2(30), 
	"BOOKING" VARCHAR2(30), 
	 CONSTRAINT "PASSANGER_UK1" UNIQUE ("BOOKING") ENABLE, 
	 CONSTRAINT "PASSANGER_CON" PRIMARY KEY ("NAME") ENABLE
   ) ;






    
  
  
