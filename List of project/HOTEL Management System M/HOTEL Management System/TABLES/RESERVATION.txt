
 
CREATE TABLE  "H_RESERVATION" 
   (	"RESERVATION_ID" NUMBER(8,0), 
	"CUSTOMER_ID" NUMBER(5,0), 
	"ROOM_ID" NUMBER(5,0), 
	"RESERVATION_DATE" DATE, 
	"DATE_IN" DATE, 
	"DATE_OUT" DATE, 
	"DATE_RANGE" DATE, 
	 CONSTRAINT "H_RESERVATION_PK" PRIMARY KEY ("RESERVATION_ID") ENABLE
   ) ;ALTER TABLE  "H_RESERVATION" ADD CONSTRAINT "H_RESERVATION_CON" FOREIGN KEY ("CUSTOMER_ID")
	  REFERENCES  "CUSTOMER_INFORMATION" ("CUST_ID") ENABLE;ALTER TABLE  "H_RESERVATION" ADD CONSTRAINT "H_RESERVATION_CON11" FOREIGN KEY ("ROOM_ID")
	  REFERENCES  "ROOM_INFORMATION" ("ROOM_ID") ENABLE;

CREATE OR REPLACE TRIGGER  "BI_H_RESERVATION" 
  before insert on "H_RESERVATION"               
  for each row  
begin   
  if :NEW."RESERVATION_ID" is null then 
    select "H_RESERVATION_SEQ".nextval into :NEW."RESERVATION_ID" from dual; 
  end if; 
end; 

/
ALTER TRIGGER  "BI_H_RESERVATION" ENABLE;

